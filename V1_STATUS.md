# V1 acceptance

**Codex live acceptance passed; Claude live acceptance is blocked by its provider's weekly limit.** Native Claude sign-in was restored and fresh subscription/model checks passed. Its first acknowledged Haiku prompt received a native `rate_limit` error before any tool call, file change or Skill invocation. The provider reported a reset at **2026-09-10 01:00 UTC / 03:00 Europe/Rome**. This is a reported reset time, not a guarantee of future availability.

RC3 corrects the hidden Claude error category and a host race that could omit the final error from live activity. Fixed error labels appear in live notices and saved history; raw provider error text remains private. The uncertain input is never automatically replayed or treated as a completed turn. [Sanitized live failure](docs/validation/v1-claude-live-rate-limit.json), [restored sign-in](docs/validation/v1-claude-restored-sign-in.json).

The approved limits remain four prompts per runtime across retries: **Codex 4/4; Claude 1/4**. Three Claude prompts remain. The unchanged acceptance sequence requires four fresh inputs for denial, approval, Skill and interruption, so it cannot complete all four checks within the remaining allowance. Do not reclaim the failed reservation or automatically retry the uncertain session.

| Requirement              | Current evidence                                                                                                                | Remaining limit                                                                                        |
| ------------------------ | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ |
| Subscription-first Codex | Real 0.153.4 / gpt-5.4-mini denial, exact approved patch, fixed tests, generation/interruption, history and exact resume passed | Provider extra usage and OS boundary remain unknown                                                    |
| Codex file permissions   | Native read-only sessions, per-patch review and disabled shell tools correct RC1's automatic-write defect                       | Supply file contents in the prompt; native repository browsing and shell/test commands are unavailable |
| Native Claude            | Official SDK 0.3.251 / CLI 2.1.251 subscription admission and native input acknowledgment observed                              | First live input hit the provider weekly limit; permission, interruption and resume acceptance pending |
| Claude Skills            | Explicit scoped/hash-bound standalone Skills and no-prompt native loading verified                                              | Live invocation remains pending; arbitrary extensions remain unsupported                               |
| History and recovery     | Codex live restart/history, exact resume and stable native turn counts passed                                                   | Detached native loaded state remains unknown; Claude uncertain inspection remains unsupported          |
| Files, diffs and usage   | Bounded inert file previews; native Codex tokens/capacity observed live                                                         | Session-start memory baseline only; no inferred charges, quota or context occupancy                    |
| Windows application      | RC3 build, development/portable startup and all archive entries verified                                                        | Portable and unsigned; no installer, signing or updater                                                |

[Start here](V1_HANDOFF.md), [live evidence](docs/validation/v1-live-results.json), [current checks](VALIDATION.md), [RC3 package and hashes](docs/validation/v1-rc3-package-results.json), [run the desktop](DESKTOP.md), [Skills compatibility](CLAUDE_SKILLS.md). RC1 and RC2 package bytes and historical evidence are preserved.

The first Codex attempt exposed automatic patch approval under native workspace-write policy. Its native launch also created a trust entry for the disposable fixture; only that exact entry was removed, with other configuration preserved. The corrected read-only launch and resumes did not create a trust entry for their fixture. RC1 remains retained as historical evidence and must not be used as the corrected package.

The corrected run proved denial before mutation and exact protected approval. A host test subprocess then failed without retained diagnostics; the cause remains unknown. Fixed tests passed independently and in the final continuation. A separate runner assertion incorrectly required an unloaded native session to report idle. The corrected comparison preserves native state as unknown, requires complete terminal turn counts and requires exact resume to return idle before sending. The final continuation passed every check, preserved earlier outcomes and ciphertext, and observed native turn counts 2 → 3 → 3. No message was replayed on resume.

The wider modular platform is not complete. OpenCode execution, remote nodes, collaboration, broader native extensions and signed distribution remain later milestones. API fallback stays disabled. The user-requested RC3 source push through `4e47763` was verified on `origin/runtime-foundation`; it is separate from application distribution.
